# List Utxos

This command will return all utxos from the wallet.

## Parameters

Below are all parameters supported to list utxos

- `wallet`: wallet name existing
- `page`: (default: 0) number of page to find
- `size`: (default: 20) number of utxo per page

## Command

Command description

```sh
cardaminal wallet utxos [wallet]
```
