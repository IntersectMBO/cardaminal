# List Wallets

In this section you can list all wallets configuration

## Parameters

Parameters supported by command

- `ignore-errors`: (optional) it will ignore errors from wallets that did corrupted

## Command

Command description

```sh
cardaminal wallet list
```
